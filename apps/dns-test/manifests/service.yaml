apiVersion: v1
kind: Service
metadata:
  name: dns-test
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/hostname: external-dns-test.andreas.mcce.werschlan.at
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: dns-test
