# Centralized BACKEND image version for ALL tenants.
# Bumping this tag rolls the backend out to every tenant that does not override
# it (override = backend-values.yaml with its own image.tag, e.g. staging).
# Versioned independently from the frontend (tenants/versions-frontend.yaml).
image:
  tag: "v1.9.0"
