replicaCount: 2

image:
  repository: europe-west3-docker.pkg.dev/sonorous-stone-498307-u2/weather-app-frontend/weather-app-frontend
  # tag comes from tenants/versions.yaml (centralized)

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 256Mi

ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    external-dns.alpha.kubernetes.io/hostname: tenant-z.inenp.werschlan.at
  hosts:
    - host: tenant-z.inenp.werschlan.at
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: tenant-z-frontend-tls
      hosts:
        - tenant-z.inenp.werschlan.at
